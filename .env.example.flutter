# Flutter Application Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# ============================================================================
# CRITICAL: Required for ALL environments
# ============================================================================

# Supabase Configuration (Flutter Frontend)
# Get these from: Supabase Dashboard → Settings → API
# NOTE: Use ANON KEY (not service role key) for Flutter client
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend API URL
# Development: http://localhost:8000
# Production: Your Railway deployment URL
API_BASE_URL=https://your-backend-api.railway.app

# ============================================================================
# OPTIONAL: Production monitoring and error tracking
# ============================================================================

# Sentry DSN (for crash reporting in production)
# Get this from: Sentry Dashboard → Settings → Projects → Client Keys (DSN)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ============================================================================
# Build Commands
# ============================================================================

# Development (local):
# flutter run --dart-define-from-file=.env

# Production Web Build:
# flutter build web --release \
#   --dart-define=SUPABASE_URL=\$SUPABASE_URL \
#   --dart-define=SUPABASE_ANON_KEY=\$SUPABASE_ANON_KEY \
#   --dart-define=API_BASE_URL=\$API_BASE_URL \
#   --dart-define=SENTRY_DSN=\$SENTRY_DSN

# Production Android Build:
# flutter build apk --release \
#   --dart-define=SUPABASE_URL=\$SUPABASE_URL \
#   --dart-define=SUPABASE_ANON_KEY=\$SUPABASE_ANON_KEY \
#   --dart-define=API_BASE_URL=\$API_BASE_URL

# Production iOS Build:
# flutter build ios --release \
#   --dart-define=SUPABASE_URL=\$SUPABASE_URL \
#   --dart-define=SUPABASE_ANON_KEY=\$SUPABASE_ANON_KEY \
#   --dart-define=API_BASE_URL=\$API_BASE_URL

# ============================================================================
# Security Notes
# ============================================================================
# 1. NEVER use default values in production
# 2. Rotate keys regularly
# 3. Use different Supabase projects for dev/staging/prod
# 4. Keep this file (.env.example.flutter) in version control
# 5. Add .env to .gitignore to prevent accidental commits
# 6. Use ANON KEY for Flutter (client-side), not SERVICE ROLE KEY
